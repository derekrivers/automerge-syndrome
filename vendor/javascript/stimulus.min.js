// Placeholder vendored by importmap-rails in a generated app. Run `bin/importmap pin @hotwired/stimulus` after bundle install to refresh.
export class Application { static start() { return { debug: false } } }
